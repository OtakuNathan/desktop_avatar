# Runtime-root channel provider manifest for the Desktop Avatar bridge.
# Discovered by ChannelEndpointProviderManager under
# <runtime_root>/channel/providers/desktop_avatar/ .
provider_id = "desktop_avatar"
entrypoint = "runtime.py"
version = "0.1.11"
enabled = true

reload_modules = ["runtime", "sidecar"]
